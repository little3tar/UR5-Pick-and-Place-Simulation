# ROS1 相关文档

本分支为 ROS2。完整的 Noetic / WSL2 踩坑记录保留在 Git 历史 `main` 分支的 `DEBUG_LOG.md` / `README.md`，
以及只读参考仓 `~/ros2/ref/UR5-Pick-and-Place-Simulation`。

仓库根目录的 `DEBUG_LOG.md` / `CHANGELOG.md` 仍可作为历史参考，不适用于 Jazzy 运行步骤。
